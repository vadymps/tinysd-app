name: Render Keep Alive
on:
  schedule:
    - cron: '*/10 7-18 * * 1-5'
  workflow_dispatch:

jobs:
  ping_job:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request
        run: curl -s -L -H "User-Agent: Mozilla/5.0" https://tinysd-api.onrender.com > /dev/null
